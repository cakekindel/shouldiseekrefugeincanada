{
  "name": "shouldiseekrefugeincanada",
  "private": true,
  "module": "index.js",
  "type": "module",
  "scripts": {
    "postinstall": "spago install",
    "serve": "docker compose up -d",
    "make": "spago build --pure",
    "test": "spago test --pure",
    "lint": "spago build --pure --strict --pedantic-packages && bun run scripts/fmt.js --check",
    "lint:fix": "bun run scripts/fmt.js",
    "bundle": "bun run scripts/bundle.server.js && bun run scripts/bundle.ui.js"
  },
  "devDependencies": {
    "bun-types": "1.1.4",
    "esbuild": "^0.23.0",
    "glob": "^11.0.0",
    "purs-backend-es": "^1.4.2",
    "purs-tidy": "^0.10.0",
    "tailwindcss": "^3.4.7",
    "typescript": "^5.0.0",
    "yaml": "^2.4.5"
  },
  "dependencies": { "mime-types": "^2.1.35" }
}
